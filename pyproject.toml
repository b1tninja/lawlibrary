[project]
name = "lawlibrary"
version = "0.2.0"
description = "Official statute publications. Sacramento, California, USA is home."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "CC0-1.0" }
dependencies = [
    "html2text",
    "mcp",
    "pycountry",
    "whoosh",
]

[project.optional-dependencies]
dev = ["pytest"]
web = ["flask"]

[project.scripts]
lawlibrary = "ca:main"
lawlibrary-mcp = "mcp_server:main"

[tool.setuptools]
py-modules = ["application", "ca", "config", "indexer", "jurisdiction", "mcp_server", "publication", "query", "utils"]
packages = ["us"]

[tool.pytest.ini_options]
testpaths = ["tests"]
